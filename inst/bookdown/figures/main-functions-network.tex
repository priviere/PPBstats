\documentclass[tikz, border=10pt]{standalone}
\input{tikz-head}

\begin{document}

\begin{tikzpicture}

  %% nodes
  \node[several-all] (FD) {format\_data\_PPBstats};
  \node[several-all] (P) [below=of FD] {plot};

  %% arrows
  \draw (FD) to (P);

  %% legend
  \node[several-all,legendkey]  (LS)  [right=of FD, xshift=6em] {};
  \node[right,legendtext] (LStext) at (LS.east) {Functions for all network format};

  %% legend bounding box
  \begin{pgfonlayer}{background}
    \node[
      fill=black!10,
      rounded corners,
      fit = (LS)(LStext)
    ] {};
  \end{pgfonlayer}


\end{tikzpicture}

\end{document}
